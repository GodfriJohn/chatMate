// Simple test without external dependencies
describe('Basic Tests', () => {
  test('math works correctly', () => {
    expect(2 + 2).toBe(4);
  });

  test('strings work correctly', () => {
    expect('hello').toBe('hello');
  });
});